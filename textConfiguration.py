# this file is used to store the path to preinstalled data, which is platform and installation dependent
import os
datasetsPath = os.path.join(os.path.split(os.path.abspath(__file__))[0], "doc", "datasets")
